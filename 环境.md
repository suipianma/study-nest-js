# 本地开发与 Docker 部署

> **双仓库：** 后端 [study-nest-js](https://github.com/suipianma/study-nest-js) + 前端 [admin-web](https://github.com/suipianma/admin-web)，与 `admin-web` **同级目录** clone。完整步骤见 **[SETUP.md](./SETUP.md)**。

项目支持 **三套用法**，不要混用端口/进程 unless 你知道在做什么。

---

## 总览


| 模式              | 用途      | 后端             | 前端                           | MySQL/Redis     |
| --------------- | ------- | -------------- | ---------------------------- | --------------- |
| **① 本地开发**      | 日常改代码   | 本机 `start:dev` | 本机 `npm run dev`             | Docker 只起 infra |
| **② 本地 Docker** | 本机验证容器化 | Docker 镜像      | 本机 `npm run dev` 或后续加 web 容器 | Docker          |
| **③ 服务器 CI/CD** | 测试/演示环境 | 服务器拉镜像         | 服务器拉镜像                       | 服务器 Docker      |


```text
③ 服务器链路：

  git push → GitHub Actions → build 前后端镜像 → push Docker Hub
                                    ↓
              服务器 docker compose pull → up -d → 自动重启
```

详细服务器步骤见 [deploy/README.md](./deploy/README.md)。

---

## 对比（① vs ②）


|             | **① 本地开发**                                   | **② 本地 Docker 全栈**                     |
| ----------- | -------------------------------------------- | -------------------------------------- |
| **适用**      | 日常改代码、断点调试                                   | 本机验证与生产一致的容器后端                         |
| **NestJS**  | 本机 `pnpm start:dev`                          | `pnpm run docker:up`                   |
| **Next.js** | 本机 `npm run dev`                             | 本机 `npm run dev`（API → localhost:3000） |
| **配置**      | `.env.dev` + `admin-web/.env.development`    | `.env.docker`                          |
| **前端 API**  | `NEXT_PUBLIC_API_BASE=http://localhost:3000` | 同上                                     |


> 切换模式前先释放 **3000** 端口（停 `start:dev` 或 `docker:down`）。

---

## ① 本地开发（推荐日常）

### 1. 准备

- Node.js 20+、pnpm、npm
- Docker Desktop
- Ollama + 模型：`ollama pull deepseek-r1:1.5b`

### 2. 只启动 MySQL + Redis

```powershell
cd study-nest-js
pnpm run docker:infra
# 等价于：docker compose up -d mysql redis
```

### 3. 初始化后端（首次）

```powershell
cd study-nest-js
pnpm install
pnpm run prisma:generate
pnpm run prisma:migrate
```

### 4. 启动后端（热更新）

```powershell
pnpm run start:dev
```

- API：[http://localhost:3000](http://localhost:3000)  
- Swagger：[http://localhost:3000/docs](http://localhost:3000/docs)

### 5. 启动前端

```powershell
cd admin-web
npm install
npm run dev
```

- 前端：[http://localhost:3001](http://localhost:3001)  
- 前端 API 配置：`admin-web/.env.development` → `NEXT_PUBLIC_API_BASE=http://localhost:3000`

### 6. 停止基础设施

```powershell
cd study-nest-js
pnpm run docker:infra:down
```

---

## ② 本地 Docker 全栈（后端进容器）

后端、MySQL、Redis 全部在 Docker；前端仍在宿主机跑（最简单）。

### 1. 准备

- Docker Desktop
- 宿主机安装 Ollama 并拉模型（AI 聊天需要）

### 2. 构建并启动

```powershell
cd study-nest-js
pnpm run docker:up
# 等价于：docker compose --profile full up -d --build
```

首次启动容器内会自动执行 `prisma migrate deploy`。

### 3. 查看后端日志

```powershell
pnpm run docker:logs
```

### 4. 启动前端（宿主机）

```powershell
cd admin-web
npm run dev
```

前端 `config/api.ts` 默认 `http://localhost:3000`，映射到 Docker 里的后端。

### 5. 停止

```powershell
cd study-nest-js
pnpm run docker:down
```

---

## 常用命令速查

```powershell
# ── 本地开发 ──
pnpm run docker:infra          # 起 MySQL + Redis
pnpm run start:dev             # 本机后端
npm run dev                    # 本机前端（admin-web 目录）

# ── Docker 全栈 ──
pnpm run docker:up             # 起 app + MySQL + Redis
pnpm run docker:logs           # 看后端日志
pnpm run docker:down           # 停全栈

# ── 数据库 ──
pnpm run prisma:migrate        # 本地 .env.dev 迁移
docker compose --profile full exec app pnpm exec prisma migrate deploy  # 容器内迁移
```

---

## ③ CI/CD（无服务器：GitHub 构建 + 本机 Docker 更新）

暂不买服务器时，同样算完整 CI/CD：

```text
本地改代码 → git push main
    → GitHub Actions 构建前后端镜像 → push Docker Hub   （CI）
    → 本机 pull 最新镜像并 docker compose up -d          （CD）
```

**详细步骤见 [deploy/README.md](./deploy/README.md)。**

### 你要配的


| 位置               | 配置                                                    |
| ---------------- | ----------------------------------------------------- |
| GitHub Secrets   | `DOCKER_USERNAME`、`DOCKER_PASSWORD`                   |
| GitHub Variables | `PUBLIC_API_BASE=http://localhost:3000`               |
| 本机 `deploy/.env` | 复制 `.env.local.example` → `.env`                      |
| 自动 CD（可选）        | 装 **Self-hosted Runner**，设 `ENABLE_LOCAL_DEPLOY=true` |
| 手动 CD            | Actions 成功后运行 `deploy/update.ps1`                     |


### 和 ① 本地开发的切换

- **开发**：`docker:infra` + `start:dev` + `npm run dev`  
- **验证 CI 产物**：停掉 3000/3001 占用 → `deploy/update.ps1` 或 compose 自动更新

以后有服务器：设 `ENABLE_SSH_DEPLOY=true`，CI 不变。

---

## 环境变量对照


| 文件                           | 用途                    |
| ---------------------------- | --------------------- |
| `study-nest-js/.env.dev`     | ① 本地开发后端              |
| `study-nest-js/.env.docker`  | ② 本地 Docker 后端        |
| `admin-web/.env.development` | ① 本地前端 API 地址         |
| `deploy/.env`（服务器）           | ③ 服务器 compose + 后端运行时 |


关键差异：

```env
# ① 本地
DATABASE_URL=...@localhost:3306/...
NEXT_PUBLIC_API_BASE=http://localhost:3000

# ② 本地 Docker 后端
DATABASE_URL=...@mysql:3306/...
OLLAMA_URL=http://host.docker.internal:11434

# ③ 服务器（构建时 + 运行时）
PUBLIC_API_BASE=http://<服务器>:3000    # GitHub Variable，构建前端镜像
CORS_ORIGIN=http://<服务器>:3001      # 服务器 .env，后端 CORS
```

---

## 常见问题

### 3306 端口被占用

本机已装 MySQL 时，Docker MySQL 起不来。可选：

1. 停本机 MySQL，继续用 Docker（推荐）
2. 改 `docker-compose.yml` 映射为 `3307:3306`，并改 `.env.dev` 里 `DATABASE_URL` 端口

### 切换方式前

- 本地后端占 3000 → 先 `Ctrl+C` 停 `start:dev`
- Docker 全栈占 3000 → 先 `pnpm run docker:down`

### Docker 里 AI 无响应

- 确认宿主机 Ollama 在跑：[http://localhost:11434](http://localhost:11434)  
- 确认已拉模型：`ollama list`  
- Linux 需 compose 里 `extra_hosts: host.docker.internal:host-gateway`（已配置）

### 两种方式数据库是否共用

共用同一 Docker 卷 `mysql_data` 时，数据是同一库。若希望完全隔离，可为 Docker 全栈单独建 compose 项目名或卷名。

---

## 新电脑最短路径

**本地开发：**

```text
装 Node + Docker + Ollama → clone
→ pnpm run docker:infra
→ pnpm install && prisma:generate && prisma:migrate
→ pnpm run start:dev + admin-web npm run dev
```

**Docker 全栈（本机）：**

```text
装 Docker + Ollama → pnpm run docker:up → admin-web npm run dev
```

**服务器：**

```text
配置 GitHub Secrets/Variables → 服务器 /opt/ai-admin 放 compose + .env
→ push main → 自动 build/push → 自动 pull 重启
```

## **一、什么在 GitHub，什么在新电脑**


| **已在 GitHub（不用在新电脑重做）** | **新电脑必须自己做**                      |
| ----------------------- | --------------------------------- |
| 源码                      | 安装 Node / Docker / Ollama / Git   |
| Actions workflow        | `pnpm install` / `npm install`    |
| Docker Hub 镜像           | 复制 `deploy/.env`（**不进 Git**）      |
| Secrets、Variables       | 若用自动 CD：重装 **Self-hosted Runner** |
|                         | 数据库数据（默认**带不过去**，见下文）             |


---

## **二、新电脑软件清单**

1. **Git**
2. **Node.js 20+**
3. **pnpm**：`npm install -g pnpm`
4. **Docker Desktop**
5. **Ollama** + 模型：`ollama pull deepseek-r1:1.5b`

---

## **三、拉代码**

git clone <你的仓库地址> NestJS

cd NestJS

仓库里已有：`.env.dev`、`admin-web/.env.development`、`deploy/.env.local.example` 等模板，**clone 后一般不用改**（本地开发）。

---

## **四、场景 A：本地开发（日常改代码）**

*# 1. 基础设施*

cd study-nest-js

pnpm install

pnpm run docker:infra          *# MySQL + Redis*

*# 2. 数据库（新电脑第一次必做）*

pnpm run prisma:generate

pnpm run prisma:migrate

*# 3. 后端（终端 1）*

pnpm run start:dev

*# 4. 前端（终端 2）*

cd ..\admin-web

npm install

npm run dev

访问：

- 前端 [http://localhost:3001](http://localhost:3001/)
- 后端 [http://localhost:3000](http://localhost:3000/)

---

## **五、场景 B：Docker 部署栈（CI 产物，pull 镜像）**

适合：不跑 `start:dev`，直接用 Docker Hub 里的镜像。

*# 1. 本机 deploy 配置（.env 不在 Git 里，要重新复制）*

cd NestJS\deploy

copy .env.local.example .env

*# 2. 拉镜像并启动（首次或更新）*

docker compose pull

docker compose up -d

*# 以后 CI 更新后*

.\update.ps1

访问同样是 **3001 / 3000**。

> *跑 compose 前，先停掉场景 A 里的* `start:dev` *和* `npm run dev`*，避免端口冲突。*

---

## **六、CI/CD 在新电脑上怎么接**

### **CI（构建推镜像）**

**不用在新电脑配**，push 到 `main` 后 GitHub 云端会自动 build + push Docker Hub。

### **CD（本机自动更新）**

旧电脑的 **Self-hosted Runner 不会跟着走**，新电脑要二选一：

**方式 1：重装 Runner（push 后自动更新 Docker）**

1. GitHub 仓库 → **Settings → Actions → Runners**
2. 旧 Runner 若不用了 → **Remove**
3. **New self-hosted runner** → 选 Windows → 按指引在新电脑安装并 `run.cmd`
4. 确认 Variable：`ENABLE_LOCAL_DEPLOY=true`

**方式 2：不用 Runner（手动 CD）**

push 后等 Actions 成功，在新电脑执行：

d:\Note\NestJS\deploy\update.ps1

---

## **七、数据库数据要不要迁**


| **数据**                | **新电脑默认**                 |
| --------------------- | ------------------------- |
| 用户/会话/消息              | **空库**（需重新注册，或自己导出导入）     |
| Docker 卷 `mysql_data` | 在本机 Docker 里，**不随 Git 走** |


要从旧电脑迁数据（可选）：

*# 旧电脑导出*

docker exec <mysql容器名> mysqldump -uroot -p ai_admin > backup.sql

*# 新电脑导入（compose 已启动 mysql 后）*

docker exec -i <mysql容器名> mysql -uroot -p ai_admin < backup.sql

---

## **八、新电脑最短清单（Checklist）**

□ 装 Git / Node / pnpm / Docker / Ollama

□ git clone

□ 选一种模式：

  【开发】

  □ study-nest-js: pnpm install → docker:infra → prisma:migrate → start:dev

  □ admin-web: npm install → npm run dev

  【Docker 部署】

  □ deploy: copy .env.local.example → .env

  □ docker compose up -d  或  update.ps1

  □ （可选）装 Self-hosted Runner

□ 浏览器打开 localhost:3001 验证

---

## **九、和旧电脑的关系**

旧电脑                          新电脑

  │                               │

  ├─ 代码 ── git push/pull ──────►│ 同一套仓库

  ├─ CI ── 在 GitHub 云端 ───────►│ 共用，不用重配 Secrets

  ├─ 镜像 ── Docker Hub ─────────►│ docker compose pull 即可

  ├─ deploy/.env ── 手动复制 ────►│ 或从 .env.local.example 重建

  ├─ Runner ── 每台机器独立 ─────►│ 新电脑重装 Runner

  └─ MySQL 数据 ── 可选 mysqldump ►│ 不迁则重新注册

---

**总结：** 新电脑 = **clone + 装依赖 + 复制** `deploy/.env`**（若用 Docker 部署）+ 按需重装 Runner**；GitHub CI 不用动，镜像从 Hub 拉即可。若你主要是开发，优先走 **场景 A** 即可。